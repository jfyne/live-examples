{
    "name": "live-npm-example",
    "version": "0.0.0",
    "type": "module",
    "description": "An example of importing the live package.",
    "scripts": {
        "prepare": "npx esbuild --bundle index.js --outfile=main.js"
    },
    "author": "",
    "license": "MIT",
    "dependencies": {
        "@jfyne/live": "^0.7.2"
    },
    "devDependencies": {
        "esbuild": "^0.8.44"
    }
}
